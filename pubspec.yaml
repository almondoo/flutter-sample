name: sample
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_launcher_icons: ^0.9.2
  flutter_lints: ^1.0.4
  url_launcher: ^6.0.17
  flutter_hooks: ^0.18.1
  hooks_riverpod: ^1.0.3
  flutter_riverpod: ^1.0.3
  sqflite: ^2.0.1
  path_provider: ^2.0.8
  path: ^1.8.0
  drift: ^1.1.0
  riverpod: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.7
  drift_dev: ^1.1.0
  drift_db_viewer: ^1.0.0

flutter:
  uses-material-design: true

flutter_icons:
  image_path: "assets/images/sample_icon.png"
  android: true
  ios: true
